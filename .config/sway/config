input * {
    xkb_layout "de,de"
    xkb_variant "neo_qwertz,nodeadkeys"
    # xkb_options "grp:win_space_toggle"
}

# TODO: Not sure if I need this
# input 10730:866:Kinesis_Corporation_Adv360_Pro_Keyboard xkb_model "pc105"

include ~/.config/sway/config.d/*

include /etc/sway/config.d/10-systemd-cgroups.conf
include /etc/sway/config.d/10-systemd-session.conf
include /etc/sway/config.d/50-systemd-user.conf
# sudo mkdir -p /etc/systemd/user-generators/ && sudo tee <<"EOF" /etc/systemd/user-generators/systemd-xdg-autostart-generator && sudo chmod +x $_
# #!/bin/sh
# export XDG_CONFIG_DIRS=
# exec /usr/lib/systemd/user-generators/systemd-xdg-autostart-generator "$@"
# EOF
include /etc/sway/config.d/95-xdg-desktop-autostart.conf

# exec_always swaymsg && systemctl --user start sway-session.target
# exec_always sh -c 'while true; do ( date; systemctl --user is-active sway-session.target; swaymsg; echo $? ) >>/tmp/session-test; sleep .2; done'

# TODO: clipboard manager
#  - wl-clip-persist --clipboard regular --all-mime-type-regex '^(?!x-kde-passwordManagerHint).+'
# TODO: wl-record?
# TODO: replace pasystray
# TODO: replace nm-applet
# TODO: new launcher? `walker --gapplication-service &`?
# TODO: keybind help
# TODO: what is fcitx5?
# TODO: autorandr replacement: kanshi? (or srandr? specific to sway though)
# TODO: arandr replacement (nwg-displays? watch out with adaptive-sync issue(only on one), deleting bg issue) https://wiki.archlinux.org/title/Sway#Change_screen_resolution_after_start
# TODO: replace https://github.com/Ventto/mons or https://github.com/chmln/enact
# TODO: Launch of Focus?

# TODO: waybar do-not-disturb indicator and switch with swaync-client

# TODO: sway as systemd-unit(s)
# TODO: uwsm-app? envs seperate?
# TODO: waybar as systemd unit
# TODO: Autostarts to systemd?

# Check autostarts
# stuff started by systemd:
# - (ssh|gpg)-agent
# - ghostty
# - swaync
# - set-background.timer
# - swayidle
# - autotiling

